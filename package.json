{
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "package": "cd dist && zip -r function.zip index.js",
    "deploy": "pnpm build && pnpm package"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.1075.0",
    "@aws-sdk/client-sns": "^3.1048.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
